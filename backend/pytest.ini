[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: Fast isolated tests with no external runtime dependencies.
    integration: Tests that exercise app/runtime integration points (DB/API/services).
    smoke: End-to-end smoke checks for critical flows.
    e2e: Full end-to-end tests that require the full stack.
